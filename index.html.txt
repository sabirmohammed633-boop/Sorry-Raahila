<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sorry Raahila 💕</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ffd6e0, #fff1f5);
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
      text-align: center;
    }

    .card {
      background: white;
      padding: 45px;
      border-radius: 25px;
      box-shadow: 0 12px 30px rgba(0,0,0,0.15);
      max-width: 420px;
      position: relative;
    }

    h1 {
      color: #ff4d6d;
      margin-bottom: 15px;
    }

    p {
      font-size: 18px;
      color: #444;
      line-height: 1.7;
    }

    .buttons {
      margin-top: 25px;
    }

    button {
      padding: 14px 28px;
      border: none;
      border-radius: 40px;
      font-size: 16px;
      cursor: pointer;
      margin: 10px;
    }

    #yesBtn {
      background: #ff4d6d;
      color: white;
    }

    #noBtn {
      background: #ccc;
      color: #333;
      position: absolute;
    }

    .promise {
      margin-top: 20px;
      font-size: 20px;
      color: #ff4d6d;
      display: none;
      animation: fadeIn 1s ease-in;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
  </style>
</head>
<body>

  <div class="card">
    <h1>Raahila 💖</h1>

    <p>
      I’m really sorry if I hurt you.  
      You mean so much to me, and I never want to lose you.
    </p>

    <p>
      Will you forgive me? 🥺
    </p>

    <div class="buttons">
      <button id="yesBtn" onclick="sayYes()">Yes 💕</button>
      <button id="noBtn" onmouseover="moveNo()">No 🙈</button>
    </div>

    <div class="promise" id="promise">
      I promise to stay with you forever ❤️
    </div>
  </div>

  <script>
    function sayYes() {
      document.getElementById("promise").style.display = "block";
      alert("Thank you Raahila 💖 I love you!");
    }

    function moveNo() {
      const noBtn = document.getElementById("noBtn");
      const x = Math.random() * 250;
      const y = Math.random() * 200;
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    }
  </script>

</body>
</html>